actrn:
  image: okibot/processors
  command: make start actrn
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

contentmine:
  image: okibot/processors
  command: make start contentmine
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

contrib:
  image: okibot/processors
  command: make start contrib
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    AWS_ACCESS_KEY_ID:
    AWS_SECRET_ACCESS_KEY:
    AWS_S3_BUCKET:
    AWS_S3_REGION:
  tags:
    - opentrials

euctr:
  image: okibot/processors
  command: make start euctr
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

fdadl:
  image: okibot/processors
  command: make start fdadl
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

fdadap:
  image: okibot/processors
  command: make start fda_dap
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    AWS_ACCESS_KEY_ID:
    AWS_SECRET_ACCESS_KEY:
    AWS_S3_BUCKET:
    AWS_S3_REGION:
    AWS_S3_CUSTOM_DOMAIN:
    DOCUMENTCLOUD_USERNAME:
    DOCUMENTCLOUD_PASSWORD:
    DOCUMENTCLOUD_PROJECT:
  tags:
    - opentrials

gsk:
  image: okibot/processors
  command: make start gsk
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

hra:
  image: okibot/processors
  command: make start hra
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

hra-linker:
  image: okibot/processors
  command: make start hra_linker
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

icdcm:
  image: okibot/processors
  command: make start icdcm
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

icdpcs:
  image: okibot/processors
  command: make start icdpcs
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

ictrp:
  image: okibot/processors
  command: make start ictrp
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

isrctn:
  image: okibot/processors
  command: make start isrctn
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

jprn:
  image: okibot/processors
  command: make start jprn
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

nct:
  image: okibot/processors
  command: make start nct
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

openaire:
  image: okibot/processors
  command: make start openaire
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

osf:
  image: okibot/processors
  command: make start osf
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    OSF_URL:
    OSF_KEY:
    OSF_NAMESPACE:
  tags:
    - opentrials

pfizer:
  image: okibot/processors
  command: make start pfizer
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

pubmed:
  image: okibot/processors
  command: make start pubmed
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

pubmed-linker:
  image: okibot/processors
  command: make start pubmed_linker
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

pybossa-indications:
  image: okibot/processors
  command: make start pybossa_indications
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    PYBOSSA_URL:
    PYBOSSA_API_KEY:
    PYBOSSA_PROJECT_INDICATIONS:
  tags:
    - opentrials

record-remover:
  image: okibot/processors
  command: make start record_remover
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

takeda:
  image: okibot/processors
  command: make start takeda
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

trial-remover:
  image: okibot/processors
  command: make start trial_remover
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

merge-trials-identifiers:
  image: okibot/processors
  command: make start merge_trials_identifiers
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials

remove-unknown-dcloud-docs:
  image: okibot/processors
  command: make start remove_unknown_documentcloud_docs
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    DOCUMENTCLOUD_USERNAME:
    DOCUMENTCLOUD_PASSWORD:
    DOCUMENTCLOUD_PROJECT:
  tags:
    - opentrials

sync-text-from-documentcloud:
  image: okibot/processors
  command: make start sync_text_from_documentcloud
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    DOCUMENTCLOUD_USERNAME:
    DOCUMENTCLOUD_PASSWORD:
    DOCUMENTCLOUD_PROJECT:
  tags:
    - opentrials

sources-remover:
  image: okibot/processors
  command: make start sources_remover
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    REMOVE_SOURCE_IDS:
  tags:
    - opentrials

remove-sources-data:
  image: okibot/processors
  command: make start remove_sources_data
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    REMOVE_SOURCE_IDS:
  tags:
    - opentrials

cochrane-reviews:
  image: okibot/processors
  command: make start cochrane_reviews
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
  tags:
    - opentrials
