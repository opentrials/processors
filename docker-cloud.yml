actrn:
  image: okibot/processors
  command: make start actrn
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

contentmine:
  image: okibot/processors
  command: make start contentmine
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

contrib:
  image: okibot/processors
  command: make start contrib
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    EXPLORERDB_URL:
    LOGGING_URL:
    AWS_ACCESS_KEY_ID:
    AWS_SECRET_ACCESS_KEY:
    AWS_S3_BUCKET:
    AWS_S3_REGION:
  tags:
    - opentrials

euctr:
  image: okibot/processors
  command: make start euctr
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

fdadl:
  image: okibot/processors
  command: make start fdadl
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

gsk:
  image: okibot/processors
  command: make start gsk
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

hra:
  image: okibot/processors
  command: make start hra
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

hra-linker:
  image: okibot/processors
  command: make start hra_linker
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

icdcm:
  image: okibot/processors
  command: make start icdcm
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

icdpcs:
  image: okibot/processors
  command: make start icdpcs
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

ictrp:
  image: okibot/processors
  command: make start ictrp
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

isrctn:
  image: okibot/processors
  command: make start isrctn
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

jprn:
  image: okibot/processors
  command: make start jprn
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

nct:
  image: okibot/processors
  command: make start nct
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

openaire:
  image: okibot/processors
  command: make start openaire
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

osf:
  image: okibot/processors
  command: make start osf
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
    OSF_URL:
    OSF_KEY:
    OSF_NAMESPACE:
  tags:
    - opentrials

pfizer:
  image: okibot/processors
  command: make start pfizer
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

pubmed:
  image: okibot/processors
  command: make start pubmed
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

pubmed-linker:
  image: okibot/processors
  command: make start pubmed_linker
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials

takeda:
  image: okibot/processors
  command: make start takeda
  # restart: always
  environment:
    WAREHOUSE_URL:
    DATABASE_URL:
    LOGGING_URL:
  tags:
    - opentrials
