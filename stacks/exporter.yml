actrn:
  image: okibot/processors
  command: python -m exporter.cli trial actrn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

euctr:
  image: okibot/processors
  command: python -m exporter.cli trial euctr
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

gsk:
  image: okibot/processors
  command: python -m exporter.cli trial gsk
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

ictrp:
  image: okibot/processors
  command: python -m exporter.cli trial ictrp
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

isrctn:
  image: okibot/processors
  command: python -m exporter.cli trial isrctn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

jprn:
  image: okibot/processors
  command: python -m exporter.cli trial jprn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

nct:
  image: okibot/processors
  command: python -m exporter.cli trial nct
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

pfizer:
  image: okibot/processors
  command: python -m exporter.cli trial pfizer
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

takeda:
  image: okibot/processors
  command: python -m exporter.cli trial takeda
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
