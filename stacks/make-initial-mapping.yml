actrn:
  image: okibot/processors
  command: python -m mapper.cli trial actrn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

euctr:
  image: okibot/processors
  command: python -m mapper.cli trial euctr
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

gsk:
  image: okibot/processors
  command: python -m mapper.cli trial gsk
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

ictrp:
  image: okibot/processors
  command: python -m mapper.cli trial ictrp
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

isrctn:
  image: okibot/processors
  command: python -m mapper.cli trial isrctn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

jprn:
  image: okibot/processors
  command: python -m mapper.cli trial jprn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

nct:
  image: okibot/processors
  command: python -m mapper.cli trial nct
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

pfizer:
  image: okibot/processors
  command: python -m mapper.cli trial pfizer
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

pubmed:
  image: okibot/processors
  command: python -m mapper.cli publication pubmed
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:

takeda:
  image: okibot/processors
  command: python -m mapper.cli trial takeda
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
    OPENTRIALS_LOGGING_URL:
