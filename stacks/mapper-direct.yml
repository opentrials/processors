script:
  image: opentrialsrobot/warehouse
  command: python -m mapper.direct.actrn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

script:
  image: opentrialsrobot/warehouse
  command: python -m mapper.direct.euctr
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

script:
  image: opentrialsrobot/warehouse
  command: python -m mapper.direct.gsk
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

script:
  image: opentrialsrobot/warehouse
  command: python -m mapper.direct.isrctn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

script:
  image: opentrialsrobot/warehouse
  command: python -m mapper.direct.jprn
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:

script:
  image: opentrialsrobot/warehouse
  command: python -m mapper.direct.nct
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DATABASE_URL:
